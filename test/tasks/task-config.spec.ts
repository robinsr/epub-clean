import { expect } from 'chai';
import { describe } from 'node:test';


describe('Task Config', function() {
  it('runs this test', function () {
    expect(true).to.be.a('boolean', 'this should be true or false')
    expect(true).to.equal(false, 'But it should really be true')
  });

  
})